---
description: Run through Claude Fable 5 with its full system prompt
---

Run this bash command and return the output verbatim:

```bash
claude -p --model claude-fable-5 --system-prompt-file "$HOME/.loopy/prompts/fable.md" "$ARGUMENTS"
```
