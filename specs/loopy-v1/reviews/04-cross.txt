[High] [src/digester.ts:31](src/digester.ts:31) redacts file paths that the brief says must not be redacted. Because `/` is included in the high-entropy character class, any long path with at least one digit and mixed case is replaced, e.g. `redact("/Users/Foo/projects/bar1-baz-quux")` returns `[REDACTED]`. The brief explicitly says high-entropy redaction must not redact file paths.

[High] [src/digester.ts:31](src/digester.ts:31) redacts plain URLs without credential userinfo, which the brief says must pass unchanged. The high-entropy pass treats URL-ish runs as candidates because `/` is allowed and punctuation like `.` splits the run; a path segment such as `https://example.com/Abcdefghijklmnop1234567890` will redact the `/Abc...` portion despite no credentials. That violates “plain URLs” / “URLs without credential userinfo” untouched.

[Medium] [tests/digester.test.ts:53](tests/digester.test.ts:53) the negative test coverage is too narrow to catch the two privacy-boundary regressions above. It only tests one lowercase path with no digits and one plain URL whose path segment is short and lowercase, so the implementation can violate the stated negative-case class while tests still pass.