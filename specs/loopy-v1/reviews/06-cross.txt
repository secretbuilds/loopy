High severity: `src/generator.ts:158`  
`validateStructure` does not enforce “EXACTLY these six `##` sections.” It only checks that the required headings appear somewhere in order. A maker output with an extra `## Notes` heading before, between, or after the required six sections will pass validation and can be written by `writeBundle` at `src/generator.ts:349`. This violates the brief’s required `loop.md` shape and weakens the “NO JUNK BUNDLES” guarantee.

Answers to the requested checks:

- A `loop.md` missing any of the six required sections cannot be written under this implementation.
- Structural validation runs before the checker.
- The revision cycle is exactly one maker retry after first failure.
- Validation/checker failures leave zero files because writing only happens after success.
- The checker is a separate runner invocation and re-asks once on unparseable output.

Fix needed before clean: reject any `##` heading list that is not exactly the six expected headings in exact order.