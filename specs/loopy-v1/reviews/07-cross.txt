High: [src/installers/shared.ts:217](src/installers/shared.ts:217) only deletes the plist file and never removes directories created during install at [src/installers/claude-code.ts:36](src/installers/claude-code.ts:36) / [src/installers/codex.ts:34](src/installers/codex.ts:34). A fresh temp HOME will retain `Library/LaunchAgents` after uninstall, violating “zero residue”, byte-identical install→uninstall, and “every touched path recorded in the manifest” because those directories are created but not tracked.

High: [src/installers/claude-code.ts:37](src/installers/claude-code.ts:37) and [src/installers/codex.ts:35](src/installers/codex.ts:35) write the plist before `launchctl load`, but the manifest is not updated until after `ctx.exec` at lines 39/37. If `ctx.exec` throws or otherwise fails before the manifest write, the installer leaves an untracked plist on disk, so `uninstallLoop` cannot reverse it from the manifest.

High: [src/installers/shared.ts:159](src/installers/shared.ts:159) silently returns on any settings parse/read error, then [src/installers/shared.ts:223](src/installers/shared.ts:223) clears `installedPaths` anyway. If `settings.json` is malformed or temporarily unreadable, uninstall leaves the loopy hook in place while marking the bundle uninstalled, violating one-call uninstall and zero-residue guarantees.

Medium: [src/installers/shared.ts:179](src/installers/shared.ts:179) removes an entire hook entry when any nested command contains the marker. If that entry also contains a user-owned hook command, uninstall deletes the user hook too. The brief emphasizes marker-based removal precision and preserving existing settings; this should remove only the loopy hook payload or only entries known to be exclusively created by the installer.

Medium: [src/installers/shared.ts:35](src/installers/shared.ts:35) accepts out-of-range cron values such as minute `99`, hour `99`, month `99`, etc. The brief defines a cron-style 5-field schedule; emitting invalid launchd `StartCalendarInterval` values can install broken schedules instead of rejecting invalid cron input.

Medium: [src/installers/claude-code.ts:33](src/installers/claude-code.ts:33), [src/installers/claude-code.ts:58](src/installers/claude-code.ts:58), and [src/installers/codex.ts:31](src/installers/codex.ts:31) interpolate `loopPath` into shell commands without quoting/escaping the path. Bundle paths with spaces, quotes, `$`, backticks, or shell metacharacters will break execution or alter the command run by launchd/hooks.