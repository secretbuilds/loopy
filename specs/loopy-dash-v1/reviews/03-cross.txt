Cross-review of task 03 (dashboard shell + CLI wiring) — performed by a fresh
adversarial Claude subagent (codex/kiro off; user kept it in Claude).

HIGH (fixed): src/dashboard/shell.ts runEffect had no try/catch/finally — a
throw from dispatchEffect (approve→install/generateBundle/LLM) or the trailing
assembleData refresh (scan→runEngine) would leave busy stuck forever (UI frozen)
plus an unhandled promise rejection from `void runEffect()`. Fix: try/catch/
finally — catch surfaces `error: <msg>` in the flash, finally clears busy +
re-renders, with a nested guard so a failing refresh can't re-freeze. Verified
in merged code; +1 test (dispatchEffect rejects on throwing runner).

LOW (accepted, not fixed): registering program.action(default) makes commander
route unknown subcommands (e.g. `loopy bogus`) to the dashboard instead of
erroring. This is the exact wiring the brief prescribed for bare-`loopy`; bare
invocation + all real subcommands + --help all work. Accepted design trade-off,
recorded not fixed.

Gate after fix: typecheck clean, 94 tests, build + --help green, no stale
companion/tui|frames imports, v1 TUI files deleted.
